// import {PaintBucket} from "lucide-react";
// import {floodFill} from "../draw";
// import {Tool, ToolHandler} from "./types";

// const PAINT_BUCKET = {
//   id: "PAINT_BUCKET",
//   icon: PaintBucket,
//   handler: {
//     onMouseDown: (ctx, point) => {
//       const {x, y} = point;
//       floodFill(ctx, x, y, ctx.fillStyle);
//     },
//     onMouseMove: () => {},
//     onMouseUp: () => {},
//     onMouseLeave: () => {},
//   } satisfies ToolHandler,
// } satisfies Tool<PaintBucketId>;

// type PaintBucketId = "PAINT_BUCKET";

// export {PAINT_BUCKET, type PaintBucketId};
