type Point = {
  x: number;
  y: number;
};

type MinMaxPoints = {
  minX: number;
  maxX: number;
  minY: number;
  maxY: number;
};

type Camera = {
  x: number;
  y: number;
  scale: number;
};

export {type Point, type MinMaxPoints, type Camera};
